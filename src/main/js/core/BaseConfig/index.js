import BaseConfig from "./BaseConfig";

import {
    getDefaultValue,
    getDomain,
    getInterpolationType,
    getScale,
    getType,
    parseTypedValue,
    validateBaseInput
} from "./helper";

export {
    BaseConfig as default,
    getDefaultValue,
    getDomain,
    getScale,
    getType,
    getInterpolationType,
    validateBaseInput,
    parseTypedValue
};
